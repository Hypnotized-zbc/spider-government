{
  "name": "demo",
  "output_dir": "output",
  "timeout": 15,
  "retries": 3,
  "delay": 1.0,
  "fmt": "json",
  "rules": [
    {
      "name": "example",
      "urls": ["https://example.com/"],
      "item_selector": "p",
      "fields": {
        "text": "p"
      },
      "required": ["text"],
      "dedup_field": "text"
    }
  ]
}
